---
title: "HW02"
output: html_document
---
#should we be doing all the assignments on R markdown
```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
```{r}
library(gapminder)
library(tidyverse)
```

```{r}
gapminder %>%
  filter(country == "Cambodia","vietnam") %>%
  select(1970)

if (gapminder)
  if (require("dplyr")) {
gapminder %>%
  filter(year == 2007) %>%
  group_by(continent)
    
if (gapminder %>%
  filter(country == "Cambodia") %>%
  select(year))
  
filtered <- gapminder %>%
  filter(year == '1970', continent == 'Asia')

gapminder1.1 <- gapminder %>% 
  dplyr::filter(country == 'Afghanistan'| country =='Vietnam' | country == 'Cambodia' && year == 1970:1979)

gapminder1.1
```

