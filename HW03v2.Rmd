---
title: "extra"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```
```{r}
library(tidyverse)
library(gapminder)
library(ggridges)
library(scales)
```
This is task two, where I get the maximum and minimum GDP per capita. The (dialogue+plot)
```{r}
gapminder %>% 
  group_by(continent) %>% 
  summarise(MinGDP=min(gdpPercap),Mincountry=country[gdpPercap==MinGDP],Minyear=year[gdpPercap==MinGDP],MaxGDP=max(gdpPercap),Maxcountry=country[gdpPercap==MaxGDP],Maxyear=year[gdpPercap==MaxGDP])%>% 
  mutate(Range=MaxGDP-MinGDP)

gapminder %>% 
  group_by(MinGDP=min(gdpPercap),MaxGDP=max(gdpPercap)) %>%
  ggplot(aes(country,year)) + 
  geom_point(color = "blue")+
  labs(x="country",y="year",
  title = "Cylinders vs horsepower",
  subtitle= "Information from mtcars"
  )
```

Task 3-use a boxplot?
```{r}
boxplot(mpg~cyl, data=mtcars, notch=TRUE,
  col=(c("gold","darkgreen")),
  main="Car MPG vs Cylinders", xlab="Miles per gallon", ylab="cylinders")
```

